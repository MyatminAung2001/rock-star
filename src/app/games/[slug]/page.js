import Details from ".";

export const metadata = {
    title: "Game Details",
    description: "Generated by create next app",
};

const Page = () => {
    return <Details />;
};

export default Page;
